{
  "name": "menuban-costmanager",
  "version": "1.0.0",
  "description": "منوبان — مدیریت هزینه، موجودی و فودکاست برای کافه و رستوران (آفلاین، بدون بک‌اند)",
  "private": true,
  "scripts": {
    "postinstall": "node scripts/copy-fonts.js",
    "dev": "esbuild src/main.ts --bundle --outfile=dist/app.js && node scripts/build-sw.js && esbuild src/main.ts --bundle --outfile=dist/app.js --servedir=. --watch",
    "build": "esbuild src/main.ts --bundle --minify --outfile=dist/app.js && node scripts/build-sw.js --minify",
    "build:sw": "node scripts/build-sw.js",
    "preview": "npx serve -l 3000 ."
  },
  "dependencies": {
    "idb": "^8.0.0",
    "chart.js": "^4.4.0",
    "workbox-core": "^7.1.0",
    "workbox-precaching": "^7.1.0",
    "workbox-routing": "^7.1.0",
    "workbox-strategies": "^7.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "vazirmatn": "^33.0.3"
  }
}
